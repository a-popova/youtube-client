import { GetBorderColorDirective } from './get-border-color.directive';

describe('GetBorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GetBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
